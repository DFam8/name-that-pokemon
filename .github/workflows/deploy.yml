name: Deploy to Plesk

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Plesk deployment
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            -X POST "${{ secrets.PLESK_DEPLOY_HOOK }}"
